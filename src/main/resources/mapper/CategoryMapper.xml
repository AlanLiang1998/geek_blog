<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.alanliang.geekblog.mapper.CategoryMapper">
    <select id="selectCategoryById" resultType="site.alanliang.geekblog.domain.Category">
        select id, name from t_category where id = #{id}
    </select>

    <select id="listByArticleCount" resultType="site.alanliang.geekblog.domain.Category">
        select tc.id, tc.name, count(ta.id) articleCount
        from t_category tc
        left join t_article ta
        on tc.id = ta.category_id
        where tc.display = 1
        group by tc.id having articleCount &gt; 0
    </select>
</mapper>